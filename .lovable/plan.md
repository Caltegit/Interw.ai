Dans le composant `QuestionMediaPlayer` (`src/components/interview/QuestionMediaPlayer.tsx`), variante `featured` pour les questions de type `audio`, supprimer le bloc `Button` avec `onClick={togglePlay}` (lignes 367–381) qui affiche le petit bouton play/pause pendant la lecture. Conserver le bouton "Lire la question" en cas de `needsManualPlay` (nécessaire pour débloquer l'autoplay sur mobile) et le spinner de chargement (`isBuffering`). Le bouton de réécoute est donc masqué, mais la lecture automatique, la barre de progression et le texte de statut restent fonctionnels.

Fichier concerné :
- `src/components/interview/QuestionMediaPlayer.tsx`